<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dltexported">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".DietTracker.DietTrackerActivity" />
        <activity android:name=".DietTracker.DietTracker_MyMealActivity" />
        <activity android:name=".DietTracker.DietTracker_AddNewMealActivity" />
        <activity android:name=".category.TrackAndManageCategoryActivity" />
        <activity android:name=".Journal.JournalActivity" />
        <activity android:name=".settings.SettingsActivity" />
        <activity android:name=".bodytracker.BodyTrackerActivity" />
        <activity android:name=".wizard.WizardActivity" />
        <activity android:name=".wizard.CustomTempleteActivity" />
        <activity android:name=".todolist.TodoListactivity" />
        <activity android:name=".SleepTracker.SleepTrackerActivity" />
        <activity android:name=".category.SubCategoryActivity" />
        <activity android:name=".todolist.EditTodoActivity"></activity>
    </application>

</manifest>